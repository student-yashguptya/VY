import React from 'react';

const Navbar = () => (
  <nav style={styles.navbar}>
    <h1>VY Software Company</h1>
  </nav>
);

const styles = {
  navbar: {
    backgroundColor: '#282c34',
    padding: '1rem',
    color: 'white',
    textAlign: 'center',
  },
};

export default Navbar;
